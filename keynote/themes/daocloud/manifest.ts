import type { ThemeManifest } from '../types';

export const daocloudTheme: ThemeManifest = {
  id: 'daocloud',
  label: 'DaoCloud',
  emoji: '🟢',
  credits: 'Adapted from DaoCloud × OpenClaw PPT template',
};
